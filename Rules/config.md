# Rules: Config

## Endpoints

- Base URL and all endpoint paths stored in `Core/Config/FakeStoreEndpoints.cs` and `Core/Config/JsonPlaceholderEndpoints.cs`
- Never hardcode URLs or paths in tests — use constants from service-specific Endpoints classes
